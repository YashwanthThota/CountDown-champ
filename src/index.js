import React from 'react';
import ReactDOM from 'react-dom';
//import the app component info
// . represents the current working directory
import App from './App'

ReactDOM.render(
  <App/>,
  document.getElementById('root')
);
